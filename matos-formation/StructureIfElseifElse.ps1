<#    La structure if elseif else permet d'exécuter du code en fonction d'une condition    Syntaxe    if(Condition(s)){                Bloc de script    }    elseif(Condition(s)){ => Facultatif        Bloc de script qui est soumis à sa condition    }    else{  => Facultatif        Bloc de script qui sera exécuté si on est pas passé dans le bloc if    }#># On déclare une note# Read-host renvoie obligatoirement une string# Donc on doit caster la variable en double (décimal)[double]$Note = Read-Host "Entrez une note"# Si la note est supérieure ou égale à 10if($Note -ge 10){    Write-Host "L'élève est admis !" -ForegroundColor Green}elseif($Note -ge 8){    Write-Host "L'élève est au rattrappage" -ForegroundColor Yellow}else{    Write-Host "L'élève est recalé !" -ForegroundColor Gray}